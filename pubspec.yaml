name: tabbar_post_list
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  bloc: ^8.1.4
  dio: ^5.4.3+1
  equatable: ^2.0.5
  flutter_bloc: ^8.1.4
  get_it: ^7.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  
  bloc_test: ^9.1.7
  http_mock_adapter: ^0.6.1
  mocktail: ^1.0.3

flutter:
  uses-material-design: true
